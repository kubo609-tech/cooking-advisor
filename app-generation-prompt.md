# コード生成プロンプト: 「大人の自炊塾」(仮称) 料理学習Webアプリ

このMDファイルは、コード生成AI(Claude Code等)に渡して単一HTMLアプリを生成させるための**指示書(プロンプト)**です。以下の内容をそのままAIへの指示として使用してください。

---

## あなたへの指示(AI宛)

あなたはフロントエンドエンジニアです。以下の要件に従い、**ブラウザ単体で動作する自己完結型のHTMLファイル1枚**(`index.html`)を生成してください。外部サーバー、ビルドツール、npmパッケージ、外部CDN、外部API通信は一切使用せず、ファイルをダブルクリックしてブラウザで開くだけで全機能が動作するようにしてください。

---

## 1. アプリの目的とターゲットユーザー

- **ターゲット**: これから単身赴任・一人暮らしを始める50代男性。料理経験は少ない〜初級レベル。
- **ゴール**: 健康的な食生活を自炊で維持できるようになること。単なるレシピ集ではなく、「初心者が本格的な料理技術を段階的に身につけられる学習ツール」として設計する。
- **トーン&マナー**: 説明は丁寧・平易(料理専門用語には必ず注釈)。文字サイズはやや大きめ、コントラストの高い配色で50代でも読みやすいUIにする。落ち着いた大人向けのデザイン(過度にポップにしない)。

---

## 2. 必須機能

### 2.1 メニュー提示(条件指定)
以下の条件をUI(セレクトボックス/ボタン群)で指定し、条件に合うメニューを1〜数件提示する。

- **国・料理ジャンル**(単一選択): 日本 / 中国 / インド / タイ / フランス / イタリア
- **食事区分**(単一選択): 朝食 / 昼食 / 夕食
- **人数**(単一選択): 1名 / 2名 / 3名 / 4名 分
- **好み・条件フィルタ**(複数選択可・任意項目):
  - 苦手食材の除外(例: 香菜/パクチー、辛い物、内臓系 など、レシピデータにタグとして持たせる)
  - 辛さレベル(控えめ / 普通 / しっかり)
  - 調理時間の目安(時短重視 / じっくり本格)
  - カロリー志向(カロリー控えめを優先表示)
- 条件に合致するレシピが複数ある場合は候補をカード一覧表示し、ユーザーが選択してレシピ詳細画面へ遷移する。

### 2.2 栄養バランス表示
- 選択人数分に換算した **エネルギー(kcal)、たんぱく質(g)、脂質(g)、炭水化物(g)、塩分相当量(g)** をレシピ詳細に表示する。
- 一汁三菜的な栄養バランスの考え方や、当該メニューの栄養面での特徴(例:「たんぱく質がしっかり摂れる高たんぱくメニューです」)を一言コメントとして表示する。

### 2.3 食材・調味料リスト
- レシピに紐づく食材と調味料を分けて表示する。
- 分量は**指定人数に応じて自動でスケーリング計算**する(基準人数のデータを保持し、倍率をかけて表示)。
- 各項目にチェックボックスを付け、買い物リスト/準備リストとして使えるようにする(チェック状態は `localStorage` に保存し、再訪時も保持)。

### 2.4 調理器具リスト
- レシピごとに必要な調理器具(例: フライパン、両手鍋、蒸し器、すり鉢 等)を一覧表示する。
- 家庭に無い可能性がある特殊な器具には、代用可能な道具を注記する(例:「蒸し器がなければ、鍋にザルを重ねて代用可」)。

### 2.5 調理手順(初心者向け)
- ステップ・バイ・ステップ形式(番号付き)で表示する。
- 各ステップに以下を含める:
  - 具体的な操作内容(平易な言葉で)
  - 目安時間(例:「中火で3分」)
  - 初心者がつまずきやすいポイントの「コツ・注意点」補足(例:「油がはねるので蓋を少しずらして使うと安全です」)
  - そのステップの状態がイメージできる**簡易イラスト(インラインSVG)**を配置(2.7参照)
- 手順の完了チェック機能(チェックすると打ち消し線になる、または濃淡が変わる)を付け、料理中に迷わないようにする。
- 専門用語(例:「隠し包丁」「アク取り」「乳化」等)が登場する場合は、用語にツールチップまたは注釈を付けて説明する。

### 2.6 対応料理ジャンル(6カ国)
日本・中国・インド・タイ・フランス・イタリアそれぞれについて、**本格的な**(簡易アレンジではなく、その国の家庭料理として本来の調理法・調味料構成に忠実な)レシピを収録する。

### 2.7 出来上がりイメージの表示
- 外部画像ファイルやAPIには依存せず、**HTML内にインラインSVGイラストとして埋め込む**(または軽量なCSS図形で表現する)ことで、完全オフラインでも出来上がりの見た目イメージを表示する。
- 各レシピに最低1点、可能であれば主要な調理ステップにも簡易カットイラストを添える。
- 写実的な精密画像である必要はなく、「皿に盛られた料理の色・形の雰囲気が伝わる簡易的なフラットイラスト」で良い(色面・円・曲線パスの組み合わせ程度で十分)。

---

## 3. 収録レシピの規模(初期スコープ)

- **各国 2〜3品 × 6カ国 = 合計 15〜18品** 程度から開始する(厳選・高品質を優先し、量は追わない)。
- 1つのレシピは、必要に応じて複数の食事区分(例: 朝食にも夕食にも使える)にタグ付けしてよい。無理に全区分×全人数の組み合わせを個別データにする必要はない(人数は分量スケーリングで対応するため)。
- レシピデータは下記3.1のデータ構造に従い、**後から追加しやすい形**(1レシピ=1オブジェクト)で実装すること。将来的な拡張を前提に、レシピ配列に要素を追加するだけで機能するようにする。

### 3.1 データ構造(例)
JavaScript内に以下のような構造でレシピデータを定義すること(項目は必要に応じて調整可):

```js
const RECIPES = [
  {
    id: "jp-001",
    name: "鶏肉と大根の煮物",
    cuisine: "japan", // japan | china | india | thailand | france | italy
    mealTypes: ["dinner"], // breakfast | lunch | dinner (複数可)
    baseServings: 2, // このデータの基準人数
    tags: {
      spiceLevel: "mild", // mild | medium | hot
      timeLevel: "standard", // quick | standard | slow
      excludeIngredients: [], // 苦手食材として除外検索に使うタグ
      lowCalorie: false
    },
    cookTimeMinutes: 40,
    nutrition: { // baseServings時点の1人分あたり
      kcal: 320, protein: 22, fat: 10, carb: 30, salt: 2.1
    },
    nutritionComment: "鶏肉と根菜でたんぱく質と食物繊維がしっかり摂れる一品です。",
    ingredients: [
      { name: "鶏もも肉", amount: 200, unit: "g" },
      { name: "大根", amount: 300, unit: "g" }
    ],
    seasonings: [
      { name: "醤油", amount: 30, unit: "ml" },
      { name: "みりん", amount: 20, unit: "ml" }
    ],
    tools: ["両手鍋", "包丁", "まな板"],
    toolNotes: { "両手鍋": "フライパンで代用可(蓋があるもの)" },
    steps: [
      {
        text: "大根は皮をむき、2cm厚さの半月切りにする。",
        timeMinutes: 5,
        tip: "面取り(角を薄く削ぐ)をすると煮崩れしにくくなります。",
        illustration: "cutting" // イラスト種別キー(3.2参照)
      }
    ],
    glossary: { "面取り": "煮崩れを防ぐため、切った野菜の角を薄く削り取ること。" },
    finishedIllustration: "simmered-dish" // 完成イメージのSVGキー
  }
  // ... 他レシピ
];
```

### 3.2 イラスト実装方針
- SVGは種類ごとに使い回せる**汎用パーツ(切る/炒める/煮る/盛り付け 等の簡易アイコン、皿+料理の完成カット)**を数種類定義し、`illustration`キーで呼び出す設計にする。レシピごとに全く別のSVGを1から描く必要はない(色や配置パラメータで多少のバリエーションを持たせる程度でよい)。

---

## 4. 画面構成(想定フロー)

1. **トップ画面**: アプリ名・簡単な説明 + 条件指定フォーム(国・食事区分・人数・好みフィルタ)+「メニューを探す」ボタン
2. **候補一覧画面**: 条件に合致するレシピをカード表示(完成イメージイラスト・料理名・カロリー・調理時間を一目で表示)。該当0件の場合は条件を緩める提案メッセージを出す。
3. **レシピ詳細画面**: 上から「完成イメージ」「栄養情報」「食材・調味料リスト(チェック可)」「調理器具リスト」「調理手順(チェック可・用語注釈付き)」の順に表示。人数変更セレクタをこの画面上にも置き、変更すると分量・栄養値がリアルタイム再計算される。
4. 画面遷移はページ遷移ではなく、SPA的に同一HTML内でJSによる表示切替(`display`切替 or 簡易ルーティング)で実装する。

---

## 5. 非機能要件

- **単一ファイル完結**: HTML/CSS/JSはすべて`index.html`1ファイル内に記述する(`<style>`、`<script>`をインライン)。外部ファイル参照・外部フォント・外部CDNは使用しない。
- **完全オフライン動作**: ネットワーク接続なしで全機能が動作すること。
- **レスポンシブ対応**: スマートフォン(単身赴任先での利用を想定)・PC双方で崩れないレイアウトにする(CSS Grid/Flexboxのみで実装、外部UIフレームワーク不要)。
- **状態保存**: 直近の好み条件設定、食材チェック状態、手順チェック状態を`localStorage`に保存し、次回起動時に復元する。
- **アクセシビリティ**: 十分なコントラスト比、ボタン等のタップ領域を大きめに確保(50代・スマホ操作を想定)。
- **軽量**: 外部画像を使わないため、ファイルサイズは数百KB程度に収まる想定。

---

## 6. 実装時の注意

- レシピデータ・SVGイラスト定義・UIロジックはコード内でセクション分けし、コメントで区切ってレシピ追加時に迷わない構成にする。
- 分量計算は `Math.round` 等で人が読みやすい単位(g, ml, 個, 大さじ/小さじ等)に丸める。少量すぎる場合の丸め誤差(例:0gになる)を避ける配慮をする。
- 初心者への配慮として、「まずはこれから」的な難易度の低いレシピにはバッジ(例:「はじめの一品」)を表示する等、学習ステップを意識した見せ方を検討する(必須ではないが推奨)。

---

## 7. 成果物

- `index.html` 1ファイル(上記すべてを内包)
- レシピは各国2〜3品、合計15〜18品を実データとして作り込む(ダミーデータではなく、実際に作れる手順・分量を記載すること)

---

## 8. 今後の拡張候補(今回のスコープ外・参考情報として記載のみ)

- レシピ追加(合計品数を増やす)
- 献立(複数品の組み合わせ)提案機能
- 買い物リストの印刷/共有機能
- 調理タイマー機能
